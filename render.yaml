services:
  - type: web
    name: asp-rental-system
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && python manage.py migrate --noinput && python manage.py collectstatic --noinput
    startCommand: gunicorn roombook.wsgi:application --bind=0.0.0.0:$PORT
    autoDeploy: true
    envVars:
      - key: DEBUG
        value: false
      - key: DATABASE_URL
        fromDatabase:
          name: asp-rental-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: PYTHON_VERSION
        value: 3.13
      - key: BASE_URL
        value: https://ai-powered-asp-room-rental-system-7.onrender.com
      - key: GOOGLE_OAUTH2_CLIENT_ID
        sync: false
      - key: GOOGLE_OAUTH2_CLIENT_SECRET
        sync: false
      - key: DJANGO_SETTINGS_MODULE
        value: roombook.settings

databases:
  - name: asp-rental-db
    plan: free
